// Local configuration — copy this file to config.local.js and fill in your values.
// config.local.js is gitignored and must NOT be committed.

var TOKORO_CONFIG = {
  workerUrl: 'https://tokoro-worker.YOUR_SUBDOMAIN.workers.dev',
  crawlerWorkerUrl: 'https://tokoro-crawler-worker.YOUR_SUBDOMAIN.workers.dev',
  crawlerApiKey: 'your-api-key-here',
  relayUrl: 'https://tokoro-query.pages.dev/',
  shortcutUrl: '', // optional: iCloud link for the Apple Shortcut (share from Shortcuts app → Copy iCloud Link)
};

// Node.js compatibility
if (typeof module !== 'undefined') module.exports = TOKORO_CONFIG;
